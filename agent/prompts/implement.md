---
description: Explore → plan → implement a change using subagents
argument-hint: "<what to build>"
---
Implement: $@

Use the subagent tool as a chain:
1. `explorer` — recon the relevant code for this task.
2. `planner` — turn `{previous}` into a concrete plan.
3. `fixer` — execute `{previous}` exactly.

Then summarize what changed and call out anything I should verify.
