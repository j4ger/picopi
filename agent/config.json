{
  "orchestrator": {
    "model": "orche",
    "thinking": "low"
  },

  "agents": {
    "planner":      { "model": "strong",   "thinking": "high",   "timeout": 600 },
    "auditor":      { "model": "strong",   "thinking": "high",   "timeout": 300 },
    "fixer":        { "model": "balanced", "thinking": "medium", "timeout": 300 },
    "explorer":     { "model": "fast",     "thinking": "low",    "timeout": 300 },
    "web-searcher": { "model": "fast",     "thinking": "low",    "timeout": 300 }
  },

  "aliases": {
    "strong":   [ "google/gemini-2.5-pro",   "anthropic/claude-opus-4-5" ],
    "balanced": [ "google/gemini-2.5-flash", "anthropic/claude-sonnet-4-5" ],
    "fast":     [ "google/gemini-2.5-flash", "anthropic/claude-sonnet-4-5" ],
    "cheap":    [ "google/gemini-2.5-flash", "anthropic/claude-sonnet-4-5" ],
    "orche":    [ "google/gemini-2.5-flash", "anthropic/claude-sonnet-4-5" ]
  },

  "compaction": {
    "model": "cheap"
  },

  "webSearch": {
    "_comment": "Drives the built-in web_search tool. provider: auto|exa|perplexity|brave|duckduckgo. API keys can be set here under apiKeys OR via env (EXA_API_KEY/PERPLEXITY_API_KEY/BRAVE_API_KEY); env wins when both are set. DuckDuckGo needs no key and is the always-on fallback. searchModel = perplexity model (default sonar). fallback: when true (default) any selected provider is tried first, then falls back to the other configured providers and finally DuckDuckGo; set false to pin exactly one provider.",
    "provider": "auto",
    "fallback": true,
    "searchModel": null,
    "summaryModel": null,
    "apiKeys": {
      "exa": null,
      "perplexity": null,
      "brave": null
    }
  },

  "title-maker": {
    "model": "cheap",
    "thinking": "off"
  }
}
